import 'package:flutter/material.dart';

class AppColors {
  // ── Primary ───────────────────────────────────────────────────────────────
  static const primary      = Color(0xFF6B1A1A);
  static const primaryLight = Color(0xFF7A0019);
  static const primaryFaded = Color(0x1A6B1A1A);

  // ── Background ────────────────────────────────────────────────────────────
  static const bgMain   = Color(0xFFE8DCC8);
  static const bgCard   = Color(0xFFEFE8D8);
  static const bgCream  = Color(0xFFF5EDE2);
  static const bgField  = Color(0xFFD4C4A8);
  static const bgPreview = Color(0xFFF2C9B8); 

  // ── Text ──────────────────────────────────────────────────────────────────
  static const textDark  = Color(0xFF3D2B1F);
  static const textBrown = Color(0xFF6B4C3B);
  static const textLight = Color(0xFF9E8C7A);

  // ── Status ────────────────────────────────────────────────────────────────
  static const success      = Color(0xFF4CAF50);
  static const successLight = Color(0xFFE8F5E9);
  static const warning      = Color(0xFFFF9800);
  static const warningLight = Color(0xFFFFF3EE);
  static const info         = Color(0xFF1565C0);
  static const infoLight    = Color(0xFFE3F2FD);
  static const teal         = Color(0xFF009688);
  static const tealLight    = Color(0xFFDFF5F0);
  static const error        = Color(0xFFE65100);
  static const defalt        = Color(0xFF9E9E9E);

  // ── Icon Backgrounds ──────────────────────────────────────────────────────
  static const iconBgBrown  = Color(0xFFEFE8D8);
  static const iconBgBlue   = Color(0xFFE3F2FD);
  static const iconBgPurple = Color(0xFFF3E5F5);
  static const iconBgOrange = Color(0xFFFFF3EE);

  // ── Misc ──────────────────────────────────────────────────────────────────
  static const white      = Color(0xFFFFFFFF);
  static const divider    = Color(0xFFE0D5C5);
  static const shadow     = Color(0x0F6B4C3B);
  static const shadowNav  = Color(0x146B4C3B);  
  static const borderFaint = Color(0xFFBCA98A);   
  static const brownshade = Color(0xFFA1887F);//300   
  static const brownshade2 = Color(0xFF795548);   
  static const brownshade3 = Color(0xFFD7CCC8);   
  static const brownshade4 = Color(0xFF8D6E63);  //400 
  static const purple = Color(0xFF9C27B0);   
  static const tauk = Color(0xFF9C27B0);   
}